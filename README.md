<!-- React Roadmap -->

# React Roadmap

এখানে এই ভিডিও সিরিজ থেকে শেখা সমস্ত গুরুত্বপূর্ণ বিষয়গুলির একটি নোট দেওয়া হলো, যা একটি মার্সডাউন ফাইলে সংরক্ষণ করা যেতে পারে:

# Chai aur React: সম্পূর্ণ রোডম্যাপ এবং মূল ধারণাগুলি

এই সিরিজটি সম্পূর্ণভাবে **React** শেখার জন্য ডিজাইন করা হয়েছে, যেখানে শুধুমাত্র মৌলিক বিষয়গুলিই নয়, বরং প্রতিটি ধারণার **গভীর অন্তর্দৃষ্টি (in-depth understanding)** এবং **প্রজেক্ট-ভিত্তিক শিক্ষা (project-based learning)**-এর উপর জোর দেওয়া হয়েছে। এটি নতুন এবং অভিজ্ঞ উভয় ডেভেলপারের জন্যই উপযুক্ত, যারা React-এর কর্মপদ্ধতি এবং প্রয়োগ সম্পর্কে বিস্তারিত জানতে চান।

## সিরিজের উদ্দেশ্য এবং শিক্ষণ পদ্ধতি
*   **আত্মবিশ্বাস বৃদ্ধি**: কোডিংয়ে আত্মবিশ্বাস তৈরি করা এই চ্যানেলের প্রধান লক্ষ্য।
*   **প্রজেক্ট-ভিত্তিক**: React-এর অনেক ধারণা প্রজেক্ট তৈরির সময় ব্যাখ্যা করা সহজ হয়, তাই এই সিরিজে **অনেক বেশি প্রজেক্ট** করা হবে।
*   **গভীরতা এবং অভিজ্ঞতা**: বহু বছরের অভিজ্ঞতা থেকে React-এর প্রতিটি ধারণার গভীরে যাওয়া হবে।
*   **সম্পূর্ণ সিরিজ**: এটি শুধুমাত্র একটি রোডম্যাপ ভিডিও নয়, বরং একটি **সম্পূর্ণ React সিরিজ**।
*   **Engagement**: ভিডিওতে প্রশ্ন জিজ্ঞাসা করা হলে মন্তব্য বিভাগে উত্তর দেওয়ার মাধ্যমে অংশগ্রহণ বাড়ানো উচিত। এতে বিষয়বস্তুর প্রতি আপনার ব্যস্ততা বাড়ে এবং শেখার প্রক্রিয়া উন্নত হয়।

## React কী এবং কেন?
*   **React একটি লাইব্রেরি**: React হলো একটি লাইব্রেরি, ফ্রেমওয়ার্ক নয়। লাইব্রেরি আপনাকে কোড লেখার ক্ষেত্রে **বেশি স্বাধীনতা** দেয় (যেমন, "cool dude" উপমা)। ফ্রেমওয়ার্কের (যেমন, Next.js, Django) কঠোর নিয়ম থাকে (যেমন, "military" উপমা)।
*   **React-এর জনপ্রিয়তা**: React বর্তমানে বাজারে **খুবই জনপ্রিয়** এবং এর প্রচুর চাহিদা রয়েছে। এটি ইউজার ইন্টারফেস (UI) তৈরির জন্য **ট্রেন্ডিং** এবং **শক্তিশালী**।
*   **জব মার্কেট**: React ডেভেলপারদের জন্য **চাকরির বাজার বেশ ভালো**। তবে, শুধু 'React Developer' না হয়ে 'JavaScript Developer' বা 'Software Developer' হওয়ার পরামর্শ দেওয়া হয়েছে।
*   **কেন React শিখব?**: React শেখা উচিত **জটিল ফ্রন্ট-এন্ড অ্যাপ্লিকেশন তৈরি এবং পরিচালনা সহজ করার জন্য**। এটি UI-এর উপাদানগুলিকে দক্ষতার সাথে সিঙ্ক করতে সাহায্য করে।
*   **React-এর জন্ম**: Facebook-এর একটি মেসেজিং অ্যাপের "ঘোস্ট প্রবলেম" (যেখানে মেসেজ পড়া সত্ত্বেও নোটিফিকেশন আইকন থেকে যেত) সমাধানের জন্য React তৈরি হয়েছিল। এই সমস্যাটি ছিল **JavaScript State** এবং **DOM UI**-এর মধ্যে সিঙ্কের অভাব। React এই সমস্যা সমাধান করে UI-তে **consistency** নিয়ে আসে।

## কখন React শিখবেন?
*   **JavaScript-এ দক্ষতা**: React শেখার আগে **JavaScript-এ সম্পূর্ণ দক্ষতা অর্জন করা অত্যাবশ্যক**। শুধুমাত্র মৌলিক বিষয়গুলি নয়, বরং JavaScript কীভাবে এক্সিকিউট হয়, এক্সিকিউশন কনটেক্সট, DOM ম্যানিপুলেশন ইত্যাদি সম্পর্কে গভীর জ্ঞান থাকা প্রয়োজন।
*   **প্রয়োজন উপলব্ধি**: আপনার যখন মনে হবে যে সহজ JavaScript দিয়ে বড় এবং জটিল প্রজেক্ট পরিচালনা করা কঠিন হচ্ছে, তখনই React শিখতে শুরু করা উচিত। ছোট প্রজেক্টের জন্য React প্রায়শই **"overkill"** হতে পারে।

## React-এর মূল ধারণা (Core Concepts)
*   **State ও UI ম্যানিপুলেশন**: React-এ সরাসরি ভেরিয়েবল পরিবর্তন করলে UI-তে তার প্রতিফলন হয় না। UI আপডেট করার জন্য **State** ম্যানেজমেন্ট শিখতে হয়।
*   **JSX**: JavaScript-এর মধ্যে HTML কোড লেখার একটি সহজ পদ্ধতি হলো JSX। এটি দেখতে HTML-এর মতো হলেও JavaScript ইঞ্জিন এটিকে একটি অবজেক্টে রূপান্তর করে।
*   **Component Reusability**: React কম্পোনেন্টগুলিকে **পুনরায় ব্যবহার** (reuse) করা সহজ করে তোলে। একই কম্পোনেন্ট বিভিন্ন স্থানে বিভিন্ন ডেটা (props) দিয়ে ব্যবহার করা যায়।
*   **Props (Properties)**: কম্পোনেন্টগুলির মধ্যে ডেটা পাস করার জন্য **Props** ব্যবহার করা হয়। Props আসলে জাভাস্ক্রিপ্ট অবজেক্টের কী-ভ্যালু পেয়ারের মতোই।
*   **State Propagation (Hooks)**: UI-তে পরিবর্তনগুলি কার্যকরভাবে ছড়িয়ে দিতে React **Hooks** ব্যবহার করা হয়।

### গুরুত্বপূর্ণ Hooks
*   **`useState`**: এটি React-এ State ভেরিয়েবল ঘোষণা এবং আপডেটের জন্য ব্যবহৃত হয়। `useState` দুটি জিনিস ফিরিয়ে দেয়: বর্তমান State ভ্যালু এবং সেই ভ্যালু আপডেট করার জন্য একটি ফাংশন (যেমন, `[counter, setCounter] = useState(0)`)। এটি UI আপডেটের জন্য অপরিহার্য।
    *   **Batch Update**: `useState` একই সাথে একাধিক আপডেটকে **ব্যাচ (batch)** করে পাঠায়। একাধিক `setCounter` কল একই সাথে একটি ব্যাচ হিসাবে গণনা করা হয় এবং স্টেট একবারই আপডেট হয়। এই সমস্যা সমাধানের জন্য `setCounter` ফাংশনের ভিতরে **কোলব্যাক ফাংশন** ব্যবহার করা উচিত (`setCounter(prevCounter => prevCounter + 1)`)।
*   **`useEffect`**: এই হুকটি কম্পোনেন্টের লাইফসাইকেল ইভেন্ট (যেমন, কম্পোনেন্ট মাউন্ট হওয়া, আপডেট হওয়া) পরিচালনা করে এবং সাইড ইফেক্ট (যেমন, API কল, DOM ম্যানিপুলেশন) পরিচালনা করতে ব্যবহৃত হয়। এটি ডিপেন্ডেন্সি অ্যারে (dependency array) গ্রহণ করে, যা নির্দেশ করে কখন প্রভাবটি পুনরায় চালানো উচিত।
*   **`useCallback`**: এই হুকটি একটি ফাংশন ডেফিনিশনকে **ক্যাশ (cache)** করতে সাহায্য করে, যাতে রিরেন্ডারের (re-renders) মধ্যে একই ফাংশন বারবার তৈরি না হয়। এটি পারফরম্যান্স অপ্টিমাইজেশনের জন্য ব্যবহৃত হয়, বিশেষত যখন ফাংশনগুলি প্রপস হিসাবে চাইল্ড কম্পোনেন্টগুলিতে পাস করা হয়।
*   **`useRef`**: এটি DOM উপাদান বা অন্যান্য পরিবর্তনশীল ভ্যালুগুলির একটি **রেফারেন্স** ধারণ করতে ব্যবহৃত হয় যা রিরেন্ডারে প্রভাবিত হয় না। এটি DOM-এর সাথে সরাসরি ইন্টারঅ্যাক্ট করার জন্য দরকারী, যেমন একটি ইনপুট ফিল্ড সিলেক্ট করা বা ক্লিপবোর্ডে অনুলিপি করা।
*   **`useContext`**: এটি **Prop Drilling** সমস্যা সমাধানের জন্য ব্যবহৃত হয়, যেখানে একটি প্রপ ডেটা অপ্রয়োজনীয়ভাবে অনেক কম্পোনেন্টের মধ্য দিয়ে পাস হয়। `useContext` ব্যবহার করে, একটি কম্পোনেন্ট সরাসরি গ্লোবাল স্টেট অ্যাক্সেস করতে পারে।
*   **`useId`**: এই হুকটি অ্যাক্সেসিবিলিটি অ্যাট্রিবিউটগুলির জন্য **ইউনিক আইডি** তৈরি করতে ব্যবহৃত হয়। এটি বিশেষভাবে উপযোগী যখন একটি কম্পোনেন্টের একাধিক ইনস্ট্যান্স তৈরি করা হয় এবং প্রতিটি ইনস্ট্যান্সের নিজস্ব অনন্য আইডি প্রয়োজন হয়।

## React-এর ইকোসিস্টেম এবং অ্যাড-অন
*   **React Router DOM**: React একক পৃষ্ঠার অ্যাপ্লিকেশন (SPA) তৈরি করে, যেখানে পাতা রিলোড হয় না। URL পরিবর্তন বা একাধিক পৃষ্ঠা পরিচালনার জন্য **React Router DOM**-এর মতো অতিরিক্ত প্যাকেজ (লাইব্রেরি) ব্যবহার করা হয়।
    *   **Link vs. NavLink**: `<a>` ট্যাগ ব্যবহার করলে পৃষ্ঠা রিলোড হয়, যা React-এর ধারণার পরিপন্থী। এর পরিবর্তে `Link` ব্যবহার করা হয়। `NavLink` `Link`-এর অতিরিক্ত বৈশিষ্ট্য সরবরাহ করে, যেমন সক্রিয় পৃষ্ঠার লিঙ্ক হাইলাইট করা।
    *   **`createBrowserRouter` ও `RouterProvider`**: রাউটিং কনফিগার করার জন্য `createBrowserRouter` ব্যবহার করা হয় এবং অ্যাপ্লিকেশনকে রাউটিং কার্যকারিতা সরবরাহ করার জন্য `RouterProvider` ব্যবহার করা হয়।
    *   **`Outlet`**: এটি নেস্টেড রাউটের জন্য ব্যবহৃত হয়। `Outlet` একটি প্যারেন্ট লেআউট কম্পোনেন্টের মধ্যে চাইল্ড রাউটের উপাদানগুলি রেন্ডার করে।
    *   **Dynamic Parameters (`useParams`)**: URL থেকে গতিশীল ডেটা (যেমন, `/users/:id`) ক্যাপচার করার জন্য `useParams` হুক ব্যবহার করা হয়।
    *   **Loaders (`useLoaderData`)**: এটি কম্পোনেন্ট রেন্ডার হওয়ার আগেই ডেটা ফেচ করতে সাহায্য করে, যা পারফরম্যান্স অপ্টিমাইজ করে।
*   **State Management Libraries**: React-এর বাই ডিফল্ট স্টেট ম্যানেজমেন্ট ক্ষমতা থাকলেও, জটিল অ্যাপ্লিকেশনের জন্য **Redux**, **Zustand** বা **Context API**-এর মতো লাইব্রেরি ব্যবহার করা হয়।
*   **Class-based Components**: React প্রথম যখন এসেছিল তখন ক্লাস-ভিত্তিক কম্পোনেন্ট ব্যবহার করা হতো। বর্তমানে **ফাংশনাল কম্পোনেন্ট** এবং Hooks ব্যবহৃত হয়। কিছু লেগ্যাসি কোডে এখনো ক্লাস-ভিত্তিক কম্পোনেন্ট দেখা যায়।
*   **Backend as a Service (BaaS)**: Appwrite, Firebase, Supabase-এর মতো পরিষেবাগুলি ব্যাকএন্ড ডেভেলপমেন্ট সহজ করে তোলে, যা সরাসরি React অ্যাপের সাথে যুক্ত করা যায়।

## React-এর পরবর্তী ধাপ (ফ্রেমওয়ার্ক)
*   React নিজে একটি **সম্পূর্ণ সমাধান নয়**; এর কিছু সীমাবদ্ধতা আছে (যেমন, SEO, ব্রাউজার রেন্ডারিং)। এই সীমাবদ্ধতাগুলি সমাধানের জন্য **Next.js**, **Gatsby**, ****Remix-এর মতো React-ভিত্তিক ফ্রেমওয়ার্ক ব্যবহার করা হয়। Next.js ফ্রন্ট-এন্ড এবং ব্যাক-এন্ড উভয়ই পরিচালনা করতে পারে।

## পরিবেশ সেটআপ (Environment Setup)
*   **VS Code**: কোড এডিটরের জন্য **Visual Studio Code**।
*   **Node.js**: JavaScript কে ব্রাউজারের বাইরে চালানোর জন্য এবং npm (Node Package Manager) ব্যবহার করার জন্য **Node.js**।
*   **Git ও GitHub**: কোড ভার্সন কন্ট্রোল এবং শেয়ার করার জন্য **Git** এবং **GitHub**।

## প্রজেক্ট তৈরির পদ্ধতি (Project Creation Tools)
*   **`create-react-app`**: React অ্যাপ্লিকেশন তৈরির একটি জনপ্রিয় ইউটিলিটি, তবে এটি **অনেক বড় (bulky)** এবং অতিরিক্ত জিনিসপত্র সহ আসে যা হয়তো সব সময় প্রয়োজন হয় না।
*   **Vite**: একটি **দ্রুত এবং হালকা (faster and lighter)** বিকল্প, যা আধুনিক React অ্যাপ্লিকেশন তৈরির জন্য সুপারিশ করা হয়।
*   **npm, npx**: Node প্যাকেজগুলি ইনস্টল ও চালানোর জন্য `npm` (Node Package Manager) এবং সরাসরি প্যাকেজ এক্সিকিউট করার জন্য `npx` (Node Package Executor)।

## ফাইল স্ট্রাকচার এবং কোর ফ্লো
*   **`package.json`**: প্রজেক্টের নাম, সংস্করণ, নির্ভরতা (dependencies), এবং স্ক্রিপ্ট (যেমন, `start`, `build`, `dev`) সংজ্ঞায়িত করে।
*   **`index.html`**: React অ্যাপ্লিকেশনের **একমাত্র HTML ফাইল**। React অ্যাপ্লিকেশন এই ফাইলের একটি `div` (যেমন, `<div id="root"></div>`) এর মধ্যে রেন্ডার হয়। এটিই **Single Page Application (SPA)** ধারণার ভিত্তি।
*   **`main.jsx` / `index.jsx`**: এটিই React অ্যাপ্লিকেশনের **মূল এন্ট্রি পয়েন্ট**। এখান থেকেই React DOM-এর `createRoot` পদ্ধতি ব্যবহার করে HTML ফাইলের `root` এলিমেন্টের মধ্যে React কম্পোনেন্ট রেন্ডার করা হয়।
*   **`App.jsx`**: সাধারণত মূল অ্যাপ্লিকেশন কম্পোনেন্ট হিসেবে ব্যবহৃত হয়, যা অন্যান্য চাইল্ড কম্পোনেন্টগুলিকে রেন্ডার করে।

## React-এর কার্যপদ্ধতি (Under the Hood)
*   **কাস্টম React তৈরি**: React কীভাবে কাজ করে তা বোঝার জন্য একটি **মিনি React লাইব্রেরি** তৈরি করা হয়েছে। এতে দেখানো হয় যে React আসলে DOM এলিমেন্ট তৈরি করে, অ্যাট্রিবিউট সেট করে এবং সেগুলিকে HTML DOM-এর ভিতরে যোগ করে।
*   **JSX Transpilation**: JSX সিনট্যাক্স (HTML-এর মতো) আসলে **Babel**-এর মতো **Bundler** দ্বারা `React.createElement()` কলে রূপান্তরিত হয়। এই `React.createElement()` একটি অবজেক্ট তৈরি করে যা React-এর অভ্যন্তরীণ ট্রি স্ট্রাকচারকে বর্ণনা করে।
*   **Evaluated Expressions**: JSX-এর মধ্যে `{}` কার্লি ব্রেসেসের মধ্যে শুধুমাত্র **JavaScript Expression** ব্যবহার করা যায়, পুরো JavaScript স্টেটমেন্ট (যেমন, `if-else` লুপ) নয়। কারণ এই এক্সপ্রেশনগুলি চূড়ান্ত মান হিসাবে মূল React অবজেক্টে যায়।

## Virtual DOM এবং Fiber (পারফরম্যান্স অপ্টিমাইজেশন)
*   **Virtual DOM**: React তার নিজের একটি **ভার্চুয়াল DOM ট্রি** তৈরি করে, যা ব্রাউজারের আসল DOM-এর একটি লাইটওয়েট প্রতিনিধি। যখন React State পরিবর্তন হয়, তখন React একটি নতুন ভার্চুয়াল DOM ট্রি তৈরি করে এবং এটিকে আগের ট্রিটির সাথে তুলনা করে।
*   **Reconciliation**: এই দুটি ভার্চুয়াল DOM ট্রি তুলনা করার প্রক্রিয়াকে **Reconciliation** বলা হয়। এটি React-এর একটি অ্যালগরিদম যা নির্ধারণ করে যে আসল DOM-এর কোন অংশগুলি আপডেট করা প্রয়োজন।
*   **Fiber**: React Fiber হলো React-এর **কোর রেন্ডারিং অ্যালগরিদম** যা Virtual DOM আপডেট করার জন্য ব্যবহৃত হয়। এটি **ইনক্রিমেন্টাল রেন্ডারিং (incremental rendering)** সক্ষম করে, যার অর্থ রেন্ডারিং কাজকে ছোট ছোট ভাগে ভাগ করে একাধিক ফ্রেমে ছড়িয়ে দেওয়া যায়।
    *   **Fiber-এর সুবিধা**: এটি অ্যানিমেশন, লেআউট এবং জেসচারের মতো UI ক্ষেত্রগুলিতে স্থিতিস্থাপকতা বাড়ায়। এটি কাজকে **পজ (pause), অ্যাবোর্ট (abort) এবং পুনরায় ব্যবহার (reuse)** করার ক্ষমতা দেয় এবং বিভিন্ন ধরনের আপডেটের জন্য **অগ্রাধিকার (priority)** নির্ধারণ করতে পারে।
    *   **Keys in Lists**: React Fiber-এর পারফরম্যান্সের জন্য লিস্ট রেন্ডার করার সময় প্রতিটি আইটেমের জন্য **ইউনিক `key` প্রপ** ব্যবহার করা অপরিহার্য।

## কাস্টম Hooks তৈরি করা
*   **ফাংশন**: কাস্টম হুকগুলি আসলে সাধারণ JavaScript ফাংশন, যা `use` উপসর্গ দিয়ে শুরু হয় (একটি প্রচলিত রীতি)।
*   **অন্তর্নিহিত হুক ব্যবহার**: কাস্টম হুকগুলি `useState` এবং `useEffect` এর মতো বিল্ট-ইন React হুকগুলি ব্যবহার করতে পারে।
*   **রিইউজেবিলিটি**: কাস্টম হুকগুলি লজিককে রিইউজেবল করে, বিশেষত API কল বা অন্যান্য স্টেটফুল লজিকের জন্য।

## Tailwind CSS ইন্টিগ্রেশন
*   **ইউটিলিটি-ফার্স্ট ফ্রেমওয়ার্ক**: Tailwind CSS একটি ইউটিলিটি-ফার্স্ট CSS ফ্রেমওয়ার্ক যা ক্লাস নেমের মাধ্যমে সরাসরি HTML-এ স্টাইল প্রয়োগ করতে দেয়।
*   **ইনস্টলেশন**: `npm install -D tailwindcss postcss autoprefixer`।
*   **কনফিগারেশন**: `tailwind.config.js` ফাইলে `content` প্রপার্টিতে ফাইল পাথ যোগ করা এবং `index.css` এ `@tailwind` ডিরেক্টিভ যোগ করা।
*   **ডার্ক মোড**: Tailwind CSS ডার্ক মোডকে সহজেই সমর্থন করে। HTML `<html>` ট্যাগে `dark` ক্লাস যোগ/বাদ দিয়ে CSS অটোমেটিকভাবে মোড পরিবর্তন করতে পারে।

## প্রজেক্টগুলি (শেখার বিষয় সহ)
1.  **বেসিক হ্যালো ওয়ার্ল্ড প্রজেক্ট**:
    *   ফাইল স্ট্রাকচার পরিষ্কার করা এবং অপ্রয়োজনীয় ফাইল মুছে ফেলা।
    *   React প্রজেক্ট রান করা এবং UI-তে পরিবর্তন দেখা।
    *   Vite এবং `create-react-app`-এর পার্থক্য বোঝা।
2.  **কাউন্টার প্রজেক্ট**:
    *   `useState` হুক ব্যবহার করে একটি কাউন্টার তৈরি করা।
    *   স্টেট পরিবর্তনের সাথে UI কীভাবে আপডেট হয় তা বোঝা।
    *   `setCounter` ফাংশনে কলব্যাক ব্যবহার করে **ব্যাচ আপডেট** সমস্যার সমাধান।
3.  **ব্যাকগ্রাউন্ড চেঞ্জার প্রজেক্ট**:
    *   `useState` ব্যবহার করে ডাইনামিক ব্যাকগ্রাউন্ড কালার পরিবর্তন করা।
    *   ইনলাইন স্টাইল (inline styles) এবং Tailwind CSS ক্লাস ব্যবহার।
    *   ইভেন্ট হ্যান্ডলিং (`onClick`) এবং প্যারামিটার পাস করার পদ্ধতি।
4.  **পাসওয়ার্ড জেনারেটর প্রজেক্ট**:
    *   `useState` ব্যবহার করে পাসওয়ার্ডের দৈর্ঘ্য, সংখ্যা এবং বিশেষ অক্ষর অনুমোদনের জন্য স্টেট ম্যানেজমেন্ট।
    *   `useEffect` হুক ব্যবহার করে পৃষ্ঠা লোডে এবং নির্ভরতা পরিবর্তনের সাথে পাসওয়ার্ড জেনারেট করা।
    *   `useCallback` ব্যবহার করে পাসওয়ার্ড জেনারেট করার ফাংশন অপ্টিমাইজ করা।
    *   `useRef` ব্যবহার করে ইনপুট ফিল্ডকে সরাসরি রেফারেন্স করা এবং ক্লিপবোর্ডে অনুলিপি করা।
    *   `Clipboard API` এবং সিলেকশন রেঞ্জ (selection range) বোঝা।
5.  **কারেন্সি কনভার্টার প্রজেক্ট**:
    *   **কাস্টম হুক** (`useCurrencyInfo`) তৈরি করে API থেকে ডেটা ফেচ করা।
    *   `useEffect` এবং `useState` ব্যবহার করে ডেটা এবং স্টেট পরিচালনা।
    *   পুনরায় ব্যবহারযোগ্য ইনপুট বক্স কম্পোনেন্ট তৈরি করা এবং প্রপস পাস করা।
    *   কারেন্সি অদলবদল (swap) করার কার্যকারিতা তৈরি করা।
    *   API ডেটা থেকে অপশনগুলি ম্যাপ করা এবং `key` প্রপসের গুরুত্ব।
    *   ইনপুট ফিল্ডে ইভেন্ট হ্যান্ডলিং (`onChange`) এবং ভ্যালু আপডেটের পদ্ধতি।
6.  **React Router Crash Course প্রজেক্ট**:
    *   `React Router DOM` ইনস্টল এবং কনফিগার করা।
    *   `Link` এবং `NavLink` এর ব্যবহার এবং `NavLink` দিয়ে সক্রিয় লিংক হাইলাইট করা।
    *   `createBrowserRouter` এবং `RouterProvider` ব্যবহার করে রাউটিং সেটআপ করা।
    *   `Outlet` ব্যবহার করে **নেস্টেড রাউট (nested routes)** এবং স্থায়ী লেআউট তৈরি করা।
    *   `useParams` ব্যবহার করে URL থেকে ডাইনামিক প্যারামিটার ক্যাপচার করা।
    *   `Loader` এবং `useLoaderData` ব্যবহার করে ডেটা ফেচিং অপ্টিমাইজ করা।
7.  **Context API (Theme Switcher) প্রজেক্ট**:
    *   **Prop Drilling** সমস্যা এবং Context API কীভাবে এটি সমাধান করে তা বোঝা।
    *   `createContext` ব্যবহার করে একটি থিম কনটেক্সট তৈরি করা।
    *   `useContext` ব্যবহার করে থিম ডেটা অ্যাক্সেস করা।
    *   **`ThemeContext.Provider`** ব্যবহার করে অ্যাপ্লিকেশনকে থিম ডেটা সরবরাহ করা।
    *   **কাস্টম হুক (`useTheme`)** তৈরি করে কনটেক্সট অ্যাক্সেস সহজ করা।
    *   Tailwind CSS এর `dark:` ইউটিলিটি ক্লাস এবং `useEffect` ব্যবহার করে ডার্ক/লাইট মোড টগল করা।

## গুরুত্বপূর্ণ টিপস এবং সেরা অনুশীলন
*   **ভিডিও সিরিজ সম্পূর্ণ করুন**: প্রতিটি ভিডিও গুরুত্ব সহকারে দেখুন এবং সম্পূর্ণ করুন।
*   **শেয়ার এবং মন্তব্য করুন**: ভিডিওগুলি সোশ্যাল মিডিয়ায় শেয়ার করুন এবং প্রশ্নগুলির উত্তর মন্তব্যে দিন। এটি আপনার শেখার এবং শিক্ষকের অনুপ্রেরণা বাড়ায়।
*   **JavaScript-এর ভিত্তি মজবুত করুন**: React শেখার আগে JavaScript-এর মূল ধারণাগুলি ভালোভাবে বুঝে নিন।
*   **ডকুমেন্টেশন পড়ুন**: React-এর অফিসিয়াল ডকুমেন্টেশন পড়ার অভ্যাস করুন।
*   **কোড ম্যানিপুলেট করুন**: কোড কপি-পেস্ট না করে নিজে পরিবর্তন করুন, ডিলিট করুন এবং পরীক্ষা করুন।
*   **ত্রুটিগুলি বুঝুন এবং ডিবাগ করুন**: কোডিং করার সময় ত্রুটি আসা স্বাভাবিক। কীভাবে ত্রুটিগুলি ডিবাগ করতে হয় তা শিখুন।
*   **কম্পোনেন্টের নাম ক্যাপিটালাইজ করুন**: React কম্পোনেন্টের নাম সর্বদা ক্যাপিটাল অক্ষর দিয়ে শুরু করা উচিত (যেমন, `Card` নয় `card`)।
*   **JSX (`.jsx`) এবং JavaScript (`.js`) ফাইল**: যে ফাইলগুলি JSX (HTML-এর মতো কোড) রেন্ডার করে সেগুলির জন্য `.jsx` এক্সটেনশন ব্যবহার করা উচিত, আর যেগুলি শুধুমাত্র JavaScript লজিক ধারণ করে সেগুলির জন্য `.js` এক্সটেনশন ব্যবহার করা উচিত।
*   **লুপে `key` ব্যবহার করুন**: React-এ লিস্ট রেন্ডার করার সময় পারফরম্যান্স অপ্টিমাইজেশনের জন্য প্রতিটি আইটেমের জন্য **ইউনিক `key` প্রপ** ব্যবহার করুন।
*   **অপ্টিমাইজেশন**: `useCallback`, `useMemo`, `Loader` এর মতো হুকগুলি ব্যবহার করে পারফরম্যান্স অপ্টিমাইজেশনের দিকে মনোযোগ দিন।
*   **রিইউজেবিলিটি**: কম্পোনেন্টগুলিকে পুনরায় ব্যবহারযোগ্য করে তৈরি করার চেষ্টা করুন।
*   **প্রপ ড্রিলিং (Prop Drilling)**: অপ্রয়োজনীয়ভাবে অনেক কম্পোনেন্টের মধ্য দিয়ে প্রপস পাস করা (Prop Drilling) এড়িয়ে চলুন; এর পরিবর্তে Context API বা অন্যান্য স্টেট ম্যানেজমেন্ট লাইব্রেরি ব্যবহার করুন।
*   **Git এবং GitHub ব্যবহার করুন**: আপনার কোড Git-এ কমিট এবং GitHub-এ পুশ করুন।

এই নোটটি React শেখার আপনার যাত্রায় একটি অমূল্য সম্পদ হিসাবে কাজ করবে।